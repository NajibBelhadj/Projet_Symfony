{% extends 'baseHome.html.twig' %}

{% block title %}new Offre{% endblock %}

{% block body %}
        <div>
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <h5 class="modal-title" style="margin-top: 10px">Ajouter un Offre
                  </h5>
                </div>
                <div class="modal-body">
                  <div class="column" id="main" >
                    <form action="{{ path("new_experience")}}" enctype="multipart/form-data" method="post">
                        <div class="form-group col-12">
                            <label for="nom">Post</label>
                            <input type="text" name="post" class="form-control" placeholder="post" required>
                        </div>
    
                        <div class="form-group col-12">
                            <label for="nom">Description</label>
                            <textarea  class="form-control" name="description" rows="4" cols="50" placeholder="Description" required></textarea>
                        </div>
                        <div class="form-group col-12">
                            <label for="nom">Date</label>
                            <input type="text" name="date" class="form-control" placeholder="Date" >
                        </div>

                        <div class="modal-footer">
                            <button type="submit" class="btn btn" style="background-color :#007bff; color: beige;">Ajouter</button>
                        </div>
                    </form>
                    {% for message in app.flashes('success') %}
                        <div class="alert alert-success"  >
                        {{ message }}
                        </div>
                    {% endfor %}
                    {% for message in app.flashes('error') %}
                        <div class="alert alert-danger"  >
                           {{ message }}
                        </div>
                    {% endfor %}
                  </div>

                </div>
                <div class="modal-footer">
                  
                </div>
              </div>
            </div>
          </div>
{% endblock %}
